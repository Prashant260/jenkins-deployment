apiVersion: v1
kind: Service
metadata:
  name: jenkins-fullstack-service
  labels:
    app: jenkins-fullstack-app
spec:
  type: NodePort
  selector:
    app: jenkins-fullstack-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30007
